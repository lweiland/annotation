Extended interface for the annotation server

We used the interface for the annotation and ranking of gist concepts

HowTo:
Add a folder "Images" with images, if you want to annotate images
Add bootstrap.css and jquery-1.js in the folder files. 
